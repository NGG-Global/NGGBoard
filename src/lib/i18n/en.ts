/**
 * English dictionary keyed by the original Hebrew interface strings.
 * A missing entry is harmless — the interface falls back to Hebrew.
 * Placeholders like {name} are substituted by t() after lookup.
 */
export const EN: Record<string, string> = {
  // Populated alongside the t() wrapping of each screen.
};
